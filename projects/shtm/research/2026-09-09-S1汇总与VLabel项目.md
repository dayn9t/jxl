# S1 双轨汇总与 VLabel 重标项目（2026-09-09）

> S1 属性轨（spark VLM 重判 8,383 对象）+ 检测轨（5 验证器共识 + VLM 定类 2,039 帧）合并为
> VLabel 人工审核项目。上游：`s1/{attr_track.jsonl, det_track.jsonl, preds/}`；合并脚本
> `s1/s1_vlabel_merge.py`（可重跑）；snapshot 全程只读（images/ 为 symlink）。

## 1. 合并规则

| 帧组 | 判定 | 框来源 | 属性来源 |
|------|------|--------|----------|
| det∩attr 交集（724） | 检测重标 + 属性重判 | det 终稿 | opening/lid 框与 m31 原框 IoU≥0.4 且类一致才挂 VLM 新值；未匹配框绑定属性标 pending(-1) |
| det_only（715） | 仅检测重标 | det 终稿 | — |
| empty（600） | 旧判空帧复检 | VLM 判真桶的框（漏检修复）；319 帧无框=确认负样本 | — |
| attr_only（2,432） | 仅属性重判 | 沿用 _m31.json 原框 | VLM 新值（重判对象 6,186） |

- det 终稿：person = yoloe∧rfdetr 双票；桶类 = la 锚框∧gdino 闸门 → spark VLM 定类（can/opening/lid/dump）；validators/vlm_verdict 留痕于 det_track.jsonl（项目文件不带，VLabel schema 无此字段）
- 属性值域/绑定沿用 m31：opening→sort+amount+illegal、lid→sort+side；**side 属性 id 由 m31 重复的 4 修正为 5**（盘点报告 §3 bug #5 在此落地）
- 交集帧属性判定的裁切基于 m31 旧框，框被 det 终稿替换/换类后不可迁移 → 作废 1,599 条（见对账）

## 2. 对账表

| 组 | 帧 | 框 | 属性对象 |
|----|----|----|----------|
| merged | 724 | 2,704 | 598 挂新值 + 357 pending |
| det_only | 715 | 2,058 | 0 |
| empty | 600 | 823 | 0 |
| attr_only | 2,432 | 6,186 | 6,186 |
| **合计** | **4,471** | **11,771** | 6,784 + 357 pending |

- 类分布：opening 5,945 / can 3,241 / lid 2,304 / dump 164 / person 117
- 与双轨原始数对齐：det 轨 2,039 帧/5,585 框（4,762+823）100% 落地；attr 轨 8,383 判定落地
  6,784（80.8%），**差额 1,599 = 交集帧原框被 det 终稿替换**（多数为 VLM 定类 opening/lid→can），非丢失
- 属性轨与旧标注一致率（审核优先级参考）：对象级 45.7%；amount 48.5%（分歧 3,402 最大）、
  side 72.6%、sort 84.6-86.5%；illegal 新判违规率 26.4%（4,867 否/1,745 是）

## 3. 项目位置与校验

- 项目：`/home/jiang/ws/trash/s1_relabel_v1/`（数据侧，不进 repo）——`meta.json5`(id 1031) +
  `vlabels/*.json5`(4,471) + `images/*.jpg`(4,471 symlink → /var/ias/snapshot/shtm)
- 格式：VLabel Rust v2.0（category+confidence 扁平；PropertyEntry{id,value,confidence}；特殊值
  pending=-1）；每帧带 sensor ROI（rois 字段）
- 校验：全量 schema 校验 PASS（类/属性绑定/值域/坐标 [0,1]）；`vlabel-convert export yolo
  --symlink` 冒烟 PASS（4,471 帧 11,771 框，导出框数与项目一致）

## 4. 下游用法

1. `vlabel-gui /home/jiang/ws/trash/s1_relabel_v1` 人工审核（重点：amount 属性 48.5% 分歧、
   merged 组 357 pending 对象、empty 组漏检新框）
2. 审核通过后 `vlabel-convert export -f yolo --symlink <project> <out>` → 与 cabin 存量合并重训（S3）
3. 属性判定置信度保留 VLM conf，可供分层采样（conf<0.9 优先复审）

## 5. Backlog

- **空帧轨剩余 669 帧**（empty_sample 任务 1,269 仅跑前 600）：待跑 la/gdino 召回 + VLM 复检后并入项目
- 交集帧作废的 1,599 条属性判定：对应新框如需属性，由人工审核时补标（pending 已就位）
