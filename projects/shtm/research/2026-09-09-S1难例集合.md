# S1 难例集合（人工兜底层，2026-09-09）

> 流程缺口修补（用户指出）：S1 双轨重标全自动直通训练，漏了人工兜底层。本报告按
> consensus-labeling skill ⑧ hardcase 模式（SGCC purge_review 先例）从 S1 双轨产物补筛
> 人工难例。构建脚本 `s1/hardcase_build.py`（可重跑，seed=42 确定性）；上游
> `s1/{attr_track.jsonl, det_track.jsonl, preds/}`；snapshot 全程只读。

## 1. 判据与实现（优先级序，(rel, obj) 去重后 reason 合并）

| # | code | 判据 | 实现 | 量（主判据计） | 建议动作 |
|---|------|------|------|------|------|
| 1a | C1_pending | det 终稿框未匹配到 attr 判定 → 属性 pending(-1) | 扫 s1_relabel_v1 vlabels | 357 | 补属性 |
| 1b | C1_lowconf | attr 任一属性 conf≤0.6（VLM 刻度 0.9/0.95 常规、0.8 常规存疑，≤0.6 极低） | attr_track，仅收已落地对象 | 526 | 确认 |
| 2 | C2_amount | amount 新旧不一致且新 conf<0.9（一致率 48.5% 分歧带） | attr_track old≠new | 397 | 确认 |
| 3 | C3_veto | det 轨 VLM not_bucket 否决框（la 锚框过 gdino 闸但 VLM 判非桶） | 重建 la_det+gdino_det 候选 × vlm_cache | 124（全量） | 删框 |
| 4 | C4_emptyfp | 空帧轨 la∪gdino 检出被 VLM 判误检（含真边界） | 重建 empty 簇 × vlm_cache，conf 降序均匀抽 | 300 / 1,362 | 删框 |
| 5 | C5_dump | dump 类全量（族弱——VLM 定类样本最少） | 扫 vlabels category=2 | 164 | 确认 |
| 6 | C6_form | 形态词典特殊形态 bagged/round/basket 各 30（检测 conf 最低端=边界） | vlm_cache form × 项目框 | 89 | 确认 |

- C3/C4 框不在 s1_relabel_v1 内（流水线已丢弃），以**追加对象**进入审核项目，预填检测器
  意见（C3=la 锚类、C4=can），opening/lid 附加属性标 pending——人工删框=确认非桶/误检，
  改类保留=推翻 VLM 否决
- C2 原始 565：461 落地（397 主判据 + 64 与 C1_lowconf 并存），104 随交集帧弃框作废；
  C1_lowconf 原始 747：526 落地，221 作废（作废口径与 S1汇总 §2 对账一致，attr 未落地
  合计 1,599）
- C6 池：det+empty 项目框 form 标注 bagged 1,840 / round 149 / basket 75，各取 conf 最低 30

## 2. 总量与构成

- **1,957 框 / 1,409 帧**（预算 ≤2,500 / ≤1,500，帧裁剪未触发）——按 1-2 小时人工审核设计
- 主判据构成：C1_lowconf 526 / C2_amount 397 / C1_pending 357 / C4_emptyfp 300 /
  C5_dump 164 / C3_veto 124 / C6_form 89；65 对象多判据并存（C1+C2 64、C1+C6 1）
- 类分布：opening 892 / lid 429 / can 408 / dump 228（含 C3/C4 预填类）
- 动作分布：确认 1,176 / 删框 424 / 补属性 357

## 3. 产物

| 产物 | 位置 |
|------|------|
| 难例清单 | `projects/shtm/s1/hardcase_list.jsonl`（rel, obj_idx, cls, reasons, action, detail） |
| VLabel 审核项目 | `/home/jiang/ws/trash/s1_hardcase_review/`（meta id 1032 + vlabels 1,409 + images symlink） |
| 拼图总览 | `projects/shtm/research/2026-09-09-S1难例拼图.jpg`（40 格判据分层抽样，1 分钟快审） |

- obj_idx = 审核项目 vlabel 内对象 id（GUI 对象列表顺序即 id 序），与清单一一对应
  （校验：1,957 条 cls 全对齐、追加对象 424、symlink 无损坏、帧集与清单一致）
- vlabels 复制自 s1_relabel_v1 对应帧**全量对象**（上下文完整），C3/C4 嫌疑框追加于帧尾

## 4. 审核操作说明

1. `vlabel-gui /home/jiang/ws/trash/s1_hardcase_review` 打开；对照 hardcase_list.jsonl
   （或拼图快审先建立直觉）
2. **删框**（C3/C4，424）：追加框默认嫌疑——确认非桶/误检即删（删后空标=负样本，图片
   不删）；实为真桶则改类保留并补属性
3. **补属性**（C1_pending，357）：pending(-1) 属性逐个填正式值（热键见 meta property_types）；
   框本身错可直接删框
4. **确认**（C2/C1b/C5/C6，1,176）：接受预填判定快速过，异议改值；特殊值 error=-3
   （检测器错误）/ exclude=-2（排除争议样本）可用
5. 审核完成即终稿：hardcase 帧的 vlabels **拷回覆盖** `s1_relabel_v1` 对应帧
   （hardcase 项目是这些帧的超集，直接覆盖即可），s1_relabel_v1 整体导出训练（S3）

## 5. Backlog

- C2 作废 104 条（92 帧）：26 帧含 C1_pending 待补对象；其余 66 帧判定随 det 终稿换类
  （opening→can 等，amount 属性不再适用）作废，S1汇总 §2 已记录，非审核缺口
- 空帧轨剩余 669 帧（S1汇总 §5 backlog）跑完后，同判据增量并入本清单 → **已跑完，见 §6**
- 审核中确认的新形态（词典 §1 待增补行）回填 `垃圾桶形态词典.md` 并走提示词反哺（skill ⑪）

## 6. Backlog 空帧轨补跑（2026-09-10，669 帧收官）

方法与判据与上轮（§4 C4_emptyfp / S1汇总 §1 empty 组）完全一致：la（LocateAnything-3B @
ds:18306，sdpa + expandable_segments）∪ gdino-tiny（本机 4060Ti）全召回 → IoU≥0.5 聚类
（代表框优先 la）→ spark qwen3.5-35b 定性（enable_thinking:false + json_object，并发 32）。
snapshot 只读；复用 `/tmp/shtm_s1/` 流水线（`20_gdino_b / 30_la_b / 40_vote_vlm_b`）。

**事故与重试（影响统计口径，必读）**：spark vLLM 10:20-11:28 故障（API 壳存活、引擎挂死，
宿主 load 14+；非本流水线问题）。故障窗口内 3,178 条判定以 vlm_error 写入缓存（占候选 89%）
——分类：3,026 条 HTTP 200 但返回体不可解析、152 条 Server disconnected。恢复后 purge 重跑
（备份 `preds/vlm_cache.jsonl.bak-poisoned`）一轮清零。**下表以重试后口径为准**，重试前数字
仅留痕：real 75 / fp 343 / error 3,401 / 有桶帧 32（不可信，已废弃）。

| 指标 | 值 | 对照上轮 600 帧 |
|---|---|---|
| 帧 | 669（empty_sample 1,269 的剩余，按 rel 去重排除已完成） | 600 |
| 召回候选（IoU≥0.5 聚类） | 3,819 | 2,188 |
| VLM 判真桶框 | **1,528（40.0%）**：can 712 / opening 498 / lid 307 / dump 11 | 823（37.6%） |
| VLM 判误检 | 2,068（54.1%） | 1,365（62.4%） |
| crop_error（<10px 极小框，确定性失败） | 223（5.8%） | 3（0.1%，本轮相机细长条框多） |
| **确认漏检帧（≥1 真桶框）** | **364 / 669 = 54.4%** | 281 / 600 = 46.8% |
| validators | la 单 558 / gdino 单 472 / 双 498 | — |

- 抽检（2 帧）：夜噪帧 183 候选全被 VLM 正确否决；真桶帧 1 真（红圆桶）1 错（电饭煲→lid）。
  VLM 定性=预填意见非终审，本批与上轮同定位：**候选真桶框作追加对象进人工审核**
- 产物：`s1/det_track_backlog.jsonl`（格式同 `det_track.jsonl`，track=empty，含 polygon/
  validators/vlm_verdict 留痕）+ `s1/stats_backlog.json`；VLM 缓存续用同文件（key 零冲突）
- 合并：669 帧 + 1,528 框并入 s1_relabel_v1 empty 组（600→1,269 帧全覆盖），VLM 判真桶框
  预填检测器意见（同 §3 C3/C4 追加对象规则），人工删框=确认误检、改类保留=推翻 VLM
