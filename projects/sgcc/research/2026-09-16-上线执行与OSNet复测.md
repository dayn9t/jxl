# 上线执行与 OSNet 复测（2026-09-16，jxl 交付执行 agent）

> 授权背景：用户授权跨 iapx 侧执行三项既定动作（upper v2 上线 / person v5 切现网 /
> OSNet v2 §7.4 验收复测）。纪律：只归档不删除、不动生产进程、拿不准停在评估步。

## 结论先行

| 动作 | 终态 | 一句话 |
|---|---|---|
| 一 upper_body v2 上线 | **DONE**（含 1 项既成事实确认） | 指纹两侧早已就位；生产 Rust 配置 09-15 已切 v2（Ignition 批裁决，本轮侦察发现）；本轮补齐 iapx 侧 TOML/默认常量/symlink 三位一体切换 + Rust 侧重分类已随 09-15 缓存重产自然完成 |
| 二 person v5 切现网 | **DONE**（生产冒烟声明待 iapx 侧执行） | md5 核对通过；生产配置 model_name + 部署树 symlink 双切；cache 两侧指纹自然失效（无手工归档/删除）；jxl 侧等价冒烟 10/10 PASS（铁证帧 + 三塌陷段抽样） |
| 三 OSNet v2 §7.4 复测 | **PARTIAL**：定标门 PASS / 管道门 FAIL | temp 通道全量重嵌入（生产 cache 只读零写入）；定标门 gap −0.084→**+0.001 翻正**（边缘）；管道门 F1 0.9375 < 0.9677——单窗外观漂移重入被 osnet 判新人（对阈值×守卫全组合不敏感 = 表征层事件）。**生产 reid 轴维持 handcrafted 不切换** |

## 侦察发现（三动作共用的关键事实）

1. **无生产进程在跑**（`ps`/`systemctl` 双确认；本机 systemd 未装 iap 单元）——全程无
   cache 写入竞态，无避让需求。
2. **iapx 仓 clean**（起点 `6054a1b`）——最小代码修改 + commit 前置条件满足。
3. **cache 指纹现状（重要，修正需求文档 §3 的 09-12 口径）**：
   - iapx Python：cfg_tag 7 分量第 5 分量 = upper_model（b6ce21e + e2a58d4，09-13 审计
     修复已落地）——「指纹缺分类器版本字段」的卡点**在需求文档书写后、本轮执行前已被
     iapx 侧解决**；
   - Rust：`upper_schema` = `{文件名 stem}@{阈}`，生产配置 `detector.json5` 直接指向
     **日期版本化文件名**（非 symlink）——文件名即指纹；
   - 真实缺口 = iapx 的 upper_model 串是 TOML 手工维护（权重经 `person_upper.pt`
     symlink 加载、指纹串与实际权重无联动）——切 symlink 不改串 = 指纹说谎。
4. **生产 live 配置**（`/var/howell/iap/v0.9/ias/sh-sgcc/n001/cfg/detector.json5`，
   mtime 09-15 20:03）：upper_model **已是 v2**（Ignition 批裁决）；model_name 为 v3
   （v4 坐姿漏检回退，待 v5）；role v3.2 已启用（report-only）。
5. **Rust 生产缓存**（MKV 旁车 `.json` × 850）：`upper_schema` 已是
   `2026-09-12_person_upper_n_v2@0.5`——**Rust 侧 v2 重分类已随 09-15 Ignition 缓存
   重产完成**，`detect_schema` 为 v3（切 v5 自动全量重检）。

## 动作一：upper_body v2 上线 — DONE

- **md5 核对**：v1 `.pt` `afab8371` / v2 `.pt` `5a3f47d0`（同字节数不同内容，确认真
  重训产物）；onnx 亦异。
- **切换（三处同 commit 语义内完成）**：
  1. iapx `configs/n001.toml` upper_model → `2026-09-12_person_upper_n_v2`（指纹轴，
     旧 cache 自然 miss）；
  2. iapx `cfg.UPPER_MODEL_DEFAULT` 同步 v2（缺省轴防漂移）+ 4 处测试跟进锁现状；
  3. 部署树 `person_upper.pt/.onnx` symlink → v2（readlink 验证）。
- **Rust 侧**：配置早于本轮已切 v2，无动作；重分类 pass 已由 09-15 缓存重产承担
  （`upper_schema` 旁车实证）。
- **iapx 侧重分类 pass**：无现成 upper-only 重分类命令（`reembed_cache` 仅放行 reid
  轴，upper 属非可换分量 fail-loud——设计如此）；下轮 `run_pipeline` 经 cfg_tag miss
  自然重检测+重分类（且本轮 person v5 + upper v2 双切换使 iapx cache 双重过期，一次
  重检测同时覆盖两轴）。
- **commit**：iapx `e72b3ab`。

## 动作二：person v5 切现网 — DONE（生产冒烟待 iapx 侧执行）

- **md5 复核**：`.pt` `a135025a19d74b7b7880c8e30a02db39` / `.onnx`
  `ee129ec3825a1196824b2010cd8b775f`——与通知单 §1.2 逐字一致。
- **切换**：
  1. 生产配置 `detector.json5` model_name → `2026-09-15_person_n_v5.onnx`（注释更新：
     v5 验收数字 + 回滚路径 v3 部署物仍在位；v4 不回用）；
  2. 部署树 `person.pt/.onnx` symlink → v5（readlink 验证）。
- **cache 失效（指纹机制，零手工动作）**：
  - Rust：`detect_schema` 含模型文件名 stem → 下轮检测自动全量重检（850 旁车不删不
    归档，miss 即重写）；
  - iapx：model_tag = `person.pt@{mtime 日期}`，stat 穿透 symlink → v5 mtime 09-15 →
    新 tag `person.pt@2026-09-15` ≠ 旧 `@2026-09-09` → 自然 miss。
- **冒烟（jxl 侧等价验证，生产 Rust 管线本机不可跑——服务未装/未跑）**：
  - 铁证帧 `09-49-01.180`（src2 07-06）：v5 ONNX 检出
    `(501.7, 177.4, 687.3, 428.4)` conf **0.913**——预期 ≈(501.7,177.5,686.3,428.0)
    conf ~.92，IoU 0.904（vs C 臂参照框）；
  - 三塌陷段（09:44-09:55 / 10:25-10:27 / 13:43-13:45）各 3 抽样帧 + 铁证 = 10 帧：
    **10/10 PASS**（IoU 0.904-0.992，conf 0.908-0.966）；
  - 口径：crop640 生产裁切窗（src2 = (255,0,640,640)）、conf 0.5、ONNX Runtime；
    C 臂（iapx v3 crop 全日缓存）为期望源。
  - **声明：36 MKV 全日生产冒烟待 iapx 侧执行**（通知单 §1.3 动作 3 完整口径）。

## 动作三：OSNet v2 §7.4 验收复测 — 两门结果见 §3.1/§3.2

### 复测通道设计（生产零写入）

- **写入边界评估（任务要求的第一步）**：`reembed_cache` **直接原子覆盖生产 cache**
  （非独立通道；§7.5「分量校验通道」指 validate_reembed_tag 机制而非独立输出）。
  且本轮 upper v2 切换后，生产 cache（v1 upper）与现 cfg（v2 upper）分量不符——
  reembed 会正确拒绝（upper 属非可换分量）→ 生产 cache 重嵌入本就被互锁在「先全量
  重检测」之后。**结论：不动生产 cache，改走 temp 通道。**
- **temp 通道**：`/mnt/data/jiang/ws/iapx/n001-ft2-retest/`——逐 cache 读生产 cache
  （只读）→ GPU 重嵌入（ft_v2，等价批内大批调用）→ 写 temp 树。temp cfg 钉
  upper_model=v1（对齐冻结检测）+ reid=osnet-x0_75-ft-v2 → validate_reembed_tag 只放
  行 reid/weight 两分量。
- **A/B 纯度**：基线（09-15 报告 gap −0.084）与本次复测消费**同一套冻结检测**
  （v3 detector + v1 upper，849 cache 全均质），唯一差异 = embedding 方案
  （handcrafted → osnet-ft-v2）。
- **harness 验证**：handcrafted 基线经本 temp 重放通道逐字复现生产 eval-report
  （F1 0.9677 / P 0.9375 / R 1.0 / 15/15 / 过切 1 / 过合并 0 / 换人 6/6 / 多余切分 2）
  ——重放通道正确性前置验证 PASS。
- **交付物核对**：`osnet_x0_75_ft_v2.pth` md5 `a33775ef…` / `.onnx` `e235228a…` 与
  通知单一致；checkpoint meta 自证（基座 = 钦定 x0_75、triplet、256 id、gap −0.0938
  = jxl 距离口径 0.457−0.363）。
- **iapx 代码接入（2 commits）**：`ac9db38` 注册 `osnet-x0_75-ft-v2`（OSNET_WEIGHTS +
  REID_KINDS + 训练 checkpoint `{model}` 键 unwrap）；生产 reid 轴维持 handcrafted
  过渡基线不变。

### 3.1 定标门（硬）：same_p5 > diff_p95 — **PASS（边缘）**

- **v2c 非对称（门基准）**：same_p5 **0.685** / diff_p95 **0.685** / gap **+0.001**——
  严格大于成立（工具 separated=True，退出码 0），但余量仅 ~0.001；
- 对照基线（同配对集、同冻结检测）：handcrafted v2c gap **−0.084** → **翻正达成**
  （微调目标）；MSMT17 基座七格全败（最优 −0.016）；
- 三表征全正：raw 单帧 −0.374（未清洗）/ v2 单帧 +0.069 / v2b 对称 +0.090 /
  v2c 非对称 +0.001——ft-v2 是**首个全表征过线**的权重（handcrafted 与基座均全败）；
- v2c 全分布分位（清洗后 same 289 / diff 39，与基线配对逐字一致）：same p1/p5/p25/
  p50/p75/p95/p99 = 0.436/0.685/0.850/0.911/0.947/0.976/0.991；diff =
  0.258/0.269/0.347/0.422/0.536/0.685/0.763；
- 交叉核验：reuse 只读复用（352 verdicts，零 VLM 调用）；
- 推荐阈（same_p5/diff_p95 中点）= **0.685**；
- 报告：`/mnt/data/jiang/ws/iapx/n001-ft2-retest/osnet-calibration-ft2.md`。

### 3.2 管道门：GT 15 窗 F1 ≥ 0.9677 且换人 0、过切 ≤2 — **FAIL（F1 单项）**

正规跑（阈 0.685 = 定标推荐）：

| 指标 | ft-v2@0.685 | 基线 handcrafted@0.8 | 门 |
|---|---|---|---|
| session F1 | **0.9375** | 0.9677 | ✗（< 0.9677） |
| P / R | 0.8824 / 1.0 | 0.9375 / 1.0 | — |
| matched | 15/15 | 15/15 | ✓ |
| 换人漏检 | **0**（6/6 对齐） | 0（6/6） | ✓ |
| 过切 | **2** ≤ 2 | 1 | ✓（贴线） |
| 过合并 | 0 | 0 | ✓ |
| 多余切分 | 3 | 2 | — |
| 全语料 sessions | 346 | 326 | — |

- **分窗口归因**：与基线逐窗对比，**唯一回归 = 07-02 src1 10-40-02 窗**（3 GT /
  4 pred，P 0.75，F1 0.857，过切 1）；13-50-03 窗的 F1 0.80/过切 1/多余切分 2
  **两轮完全相同**（既有弱点，非 osnet 引入）；其余窗口逐字一致。
- **根因（帧级嵌入实证）**：10-41-24→10-41-32 的 8s 空档（GT 判同一 visit 持续至
  10-43-42）——空档两侧同人余弦仅 **0.36-0.58**（低于定标 same p1 0.436 的尾部
  事件），且 A 侧末三帧自身帧间余弦 0.33-0.39（外观剧烈漂移期，B 侧恢复后
  0.97+）。osnet-ft 把姿态剧变重入判为新人 → track 切换 → 过切。handcrafted
  HSV 直方图对此钝感（> 0.8 续接）。
- **参数免疫实证**：(关联阈 0.50/0.55/0.60/0.65/0.685/0.72) × (吸收守卫
  0.30/0.40/0.45/0.60) 全组合 F1 恒 0.9375——切分是**表征层事件非参数层**；
  阈 0.45 恶化（F1 0.875、换人 4/6、过合并 2 = 跨人污染），0.50 为运行地板。
- **benchmark 六合并组**：无改善亦无回归——G3（09-40-13 起身换位 bbox 跳变，
  v3 显式标注「待 OSNet」）与 G1 在 ft-v2 下仍未合并（维持 4/6，未达 5-6/6）；
  G3 期望的「osnet 修 HSV 0.69<0.8 漏匹配」未兑现。
- **处置（按 §7.5 流）**：门未过 → **生产 reid 轴维持 handcrafted 过渡基线**
  （本轮未动 n001.toml reid 轴，仅注册变体）→ 回裁决层。候选后续：v2 微调加
  「姿态漂移正对」（07-02 10-41 类样本入 train）；或消费侧时域平滑吸收该尾部。

## 产物清单

- iapx commits：`e72b3ab`（upper v2 切换）、`ac9db38`（osnet ft-v2 注册 + 训练
  checkpoint unwrap）——均注明授权背景
- 部署树：`person.pt/.onnx`→v5、`person_upper.pt/.onnx`→v2（readlink 已验证）
- 生产配置：`/var/howell/iap/v0.9/ias/sh-sgcc/n001/cfg/detector.json5`（model_name → v5）
- temp 复测通道：`/mnt/data/jiang/ws/iapx/n001-ft2-retest/`（ft_v2 重嵌入 cache 849 +
  sessions + eval-report + osnet-calibration-ft2.md + scripts/ 五脚本存档）、
  `n001-ft2-baseline/`（handcrafted 对账用，硬链零拷贝）
- 生产侧零写入实证：生产 cache cfg_tag 仍 `handcrafted|2026-09-10_person_upper_n`
  （复测后核）、生产 sessions/state 未动、Rust 旁车未动
- jxl 仓改动：本报告 + 需求文档 §8 触发表（不 commit，主会话收官统一）

## 附：遗留观察（供裁决层）

1. **v2c 表征是 ft-v2 三表征中最弱的**（+0.001 vs v2b +0.090）——而 v2c 是生产
   「新帧 × EMA gallery」镜像、验收门基准。若下轮微调，same 尾部（p1 0.436 以下
   的姿态漂移族）是首要样本面。
2. **融合地板线 0.8**（AssocParams.fusion_floor，handcrafted 定标）在 osnet 刻度下
   截掉 same 分布 ~8-10% 帧的 gallery 融合——本轮未调（非 §7.5 声明的重定标轴），
  若下轮复测仍差可纳入敏感性面。
3. upper v2 + person v5 双切换后，iapx Python cache 双重过期——下轮 iapx
   `run_pipeline` 将全量重检测（GPU ~8h 级），重分类 pass 随之自然完成（无需单独
   命令）；Rust 侧则由 detect_schema/upper_schema 指纹自动重检重分类。
