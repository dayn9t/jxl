# person v3 重复检测归因报告（iapx 登记缺陷 → 已裁决，2026-09-12）

> 回应 `projects/sgcc/iapx-分类器需求与样本供给.md` §5 登记缺陷：同帧亚像素重合重复
> 检测（52 帧 exact 对 IoU≥0.99 + 194 对 near 0.95-0.99，60/177 cache 受染，含人帧
> 0.18%）。复现实验 + 数据集扫描已完成，**归因闭环：训练数据近重复 GT → one-to-one
> 头学会重复发射**。非导出问题、非推理封装问题、非 NMS（YOLO26 本无 NMS）。

## 1. 排除法实验（3 个证据帧，本地复现）

部署物 `2026-09-09_person_n.pt/.onnx`，复现 iapx 同款调用（crop640 裁切 + conf=0.5）：

| 证据帧 | PT 输出 | ONNX 输出 | 结论 |
|---|---|---|---|
| 2/11-30-40.010 | 2 dets（0.7735/0.5060 亚像素对） | 2 dets（0.7744/0.5036） | 一致复现 |
| 1/08-43-43.079 | 3 dets（1 正常 + 0.7438/0.5526 对） | 3 dets（同构） | 一致复现 |
| 1/08-45-57.109 | 3 dets（1 正常 + 0.7953/0.5843 对） | 3 dets（同构） | 一致复现 |

- PT ≡ ONNX（conf 差 <0.002，bbox 亚像素一致）→ **导出链路洗清**
- iapx 推理封装 = 裸 `ultralytics YOLO.predict(crop, conf=0.5)`，零自定义后处理
  （`iapx/pipeline/detect.py`）→ **封装洗清**
- 重复对模式固定：同目标一高一低 conf（低侧 0.50-0.58，贴 0.5 阈）→ **one-to-one
  头重复发射**，唯二可能是「头固有偶发」或「数据教会」，见 §2

## 2. 训练数据扫描：重复 GT 实锤（dataset_v3）

全量扫 YOLO 标签同帧近重复对：

| split | 文件 | IoU≥0.99 对 | 0.95-0.99 对 |
|---|---|---|---|
| train | 11,030 | **73** | 264 |
| val | 1,379 | 9 | 41 |
| test | 1,379 | 7 | 35 |

样例特征（8 对全查）：全部同一人两条框、归一化坐标差 0.001-0.002（640px 下 1-2px），
集中于 **cam1 2026-06-22** 各时段——共识标注管线的「检测框聚类/共识写出」层把多源
同目标写成两条（聚类阈未覆盖 0.95+ 近重复），`consensus_dataset.py merge()` 有跨层
同帧冲突检测但**无帧内近重复 GT 守卫**（invalid state 可表示）。

训练集重复 GT 文件率 0.66%（73/11,030）与推理端重复帧率 0.18% 同数量级——头从数据
学到「同人可发两条」，仅部分触发。因果链闭环。

## 3. 处置裁决

| 层 | 裁决 |
|---|---|
| 消费端（iapx） | **维持现有防御**（IoU≥0.95 保高 conf，commit 12b2b96）——充分，勿撤 |
| 数据侧（治本） | 清洗 dataset_v3 近重复 GT（IoU≥0.95 同类合并，保留 consensus 置信侧）→ **person 下次重训捎带**；重训后用 iapx 52 exact + 194 near 清单回归，预期 dup 清零 |
| 守卫（防再发） | `consensus_dataset.py` 增加帧内近重复 GT 校验（IoU≥0.95 即 fail，No Silent Degradation）；上游共识写出层同加聚类后去重 |
| ONNX 图内去重 | **不做**（图内 NMS 类后处理与 end-to-end 架构相悖，复杂度/收益差） |

## 4. 与本文件其他任务的合流

- 清洗脚本独立成工具（`jxl.bin` 候选：`dedup_gt_boxes`），v3 洗出的副本框清单同时
  供毒标注审计参考（同一融合缺陷可能喂过其他近重复形态）
- 需求 A（upper_body 重训）为分类任务不受此影响；person 检测器下次重训窗口由本缺陷
  + iapx 侧新日期样本共同触发

## 5. 产物

- 复现脚本输出（3 帧 × PT/ONNX det 表）见本文 §1；证据帧抽自
  `/mnt/data/jiang/ws/iapx/n001/cache/`（只读未动）
- 复现用帧暂存 `/tmp/iapx_dup/`（tmp 丢弃即可，证据以 cache JSON 为准）

## 6. v4 回归验证（2026-09-13 收割）

person_n001_v4（dataset_v3 + `dedup_gt_boxes --apply`，858 对清洗/834 框删除）200ep
@sgcc0 训成，双口径回归：

1. **test mAP50-95 = 0.9005**（v3 基线 0.8855，**+0.015**，远超 ±0.005 合格带）——
   清洗 GT 不但无回归反而提升
2. **重复对回归**（`gencheck/v4_dup_regression.py`，部署口径 crop_rect 推理，CPU）：
   受染清单（今日全量重扫 = 849 cache / 1,267 受染帧，exact 279 + near 988；3 证据帧
   conf 逐位一致证明与 09-12 快照口径等价）→ v4 重推理 **new pairs = 7**（exact 2 +
   near 5，清零率 99.45%）。残余 7 对 conf 均伴 <0.85 弱框，iapx 消费端防御
   （IoU≥0.95 保高 conf）能力覆盖
3. **框数骤降核查**（防漏检假阴性）：受染帧 v3→v4 检出 3,133→1,213；v4 零检出帧 613，
   逐帧核对 v3 框 1,230 个中 **ub=false 占 1,229（99.9%）**——清除对象是台面/下半身 FP
   （§5 附带发现的同类），唯一 1 个 ub=true 真人框为单帧抖动，相邻 12 帧部署口径
   全部正常检出（conf 0.61-0.84），session 层无影响
4. 部署候选已 stage：`/opt/howell/iap/v0.9/ias/model/2026-09-13_person_n.pt/.onnx`
   （已获批准，09-13 23:19 切换上线）

### 6.1 新日期共识 GT 评估（2026-09-13，无人工标注闭环）

多 VLM 共识管线（4 模型投票，池与规则见 `src/jxl/bin/vlm_pool.py`）构造 3 个新日期
验证窗 GT（07-03/07-06/06-22 各 top-3 分钟窗，559 帧，trusted 2,123 框，人眼抽检 9/9 全对），
部署口径（crop_rect 685,0,640,640）评估：

| 日期 | v3 GT_recall@0.5 | v4 | v4 提升 |
|---|---|---|---|
| 07-03 | 0.898 | **0.911** | +1.3pt |
| 07-06 | 0.877 | **0.963** | +8.7pt |
| 06-22 | 0.850 | **0.910** | +6.0pt |

- **v4 在新日期全面占优**（与 §6 test 回归同向），部署价值再加一条
- 召回缺口（9-15%）人眼裁决：miss 集中于**隔玻璃+反光区坐姿人物**（单一困难形态，
  06-22 的 12 miss 中抽 2 帧为同一玻璃后个体连续帧）——非系统性缺类
- 处置：v5 重训时把此类样本补入（共识 GT 的 miss 框即现成标注）；短期不阻塞 v4 上线
- 口径教训：全帧 OOD 评估曾给出 0.15-0.35 的假召回——**评估必须在部署域**（ROI 内）

### 6.2 v5 玻璃反光重训收割（2026-09-14，数据=唯一差异）

v5 = v4 数据集原样 + `v5_glasspack.jsonl`（共识 GT 帧 426 帧入 train，全帧按
crop_rect 裁部署域，build 见 `gencheck/build_v5_dataset.py`；glasspack 口径注意：
实际为「非 FP 帧全集」而非严格 miss 集——上游 eval fps 列表截断 50 条/日，见脚本
docstring 2026-09-15 audit 注记）；超参照 v4，val/test 同集。

**test（同口径可比）**：v4 0.9005 → v5 **0.8945**（−0.6pt，噪声带边缘）

**玻璃反光专项**（07-03 部署口径 157 GT，`gencheck/gt_eval_deploy_2026-07-03_v5.json`）：

| 权重 | GT_recall@0.5 | suspected_fp |
|---|---|---|
| v3 | 0.898 | 154 |
| v4（现役） | 0.911 | 138 |
| v5 | **0.930**（+1.9pt，miss 14→11） | 159（+21） |

- **专项有效**：玻璃反光 miss 修回 3 帧 GT；**代价**：FP +21（+15%）、整体 test −0.6pt
  ——难例重加权使决策面更激进
- **裁决建议（2026-09-14）**：v4 保持现役不动；v5 存档为候选（`runs/person_n001_v5/`，
  best.pt 已取回本地）。若线上对玻璃反光 miss 敏感（漏检代价 > 误检代价），可切 v5 换取
  召回；切换是部署决策，待用户拍板。三方可复评命令：
  `python -m jxl.bin.consensus_gt_eval_det <gt_trusted> --det v3 --det v4 --det v5 --crop-rect "685,0,640,640"`
