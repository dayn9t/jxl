# vdt 演示 CLI 与完整可视化 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `vdt run video --out-video demo.mp4` 一条命令输出完整跟踪演示视频（框+ID 稳定配色+pose 骨架+轨迹尾迹+HUD），零配置（智能默认 iou+pose 全开），选项可精细控制。

**Architecture:** 新建 `src/jxl/vdt/draw.py` 可视化库（Functional Core，纯绘制函数 + 可独立单测）；`cli.py` 升级为薄消费者（`_default_config` 工厂 + `--config` 可选 + `render_video` 替换 `annotate_video` + 可视化选项开关）。

**Tech Stack:** Python 3.12 / pydantic / opencv_python (cv2) / numpy / typer / pytest（内联 test_*，按文件发现）。基于已实现的 vdt 模块（`Tracks`/`D2dObject`/`Keypoints`/`Rect`/`OcvDecoder`）。

## Global Constraints

- **Python 3.12，src/ layout，hatchling+uv**；venv 在 `.venv/`。
- **pydantic BaseModel** 作数据模型；状态ful 对象（TrailBuffer）显式避免 pydantic + docstring 注明。
- **完整类型注解，`src/jxl/bin` 外零 `Any`**；mypy strict-ish（项目 `pyproject.toml` 配置）通过。
- **No Silent Degradation**：资源缺失/非法参数 → 具体异常（`ModelLoadError`/`BadParameter`），fail-fast，不静默回退。
- **内联 `test_*`**（pytest 按文件发现）：`.venv/bin/python -m pytest src/jxl/vdt/<file>.py`；重依赖（cv2 已是依赖，模块级 import 可，与 `decoder.py` 一致）。
- **cv2 在 mypy override 列表**：cv2 常量/函数 attr 访问若 mypy 报 `attr-defined`（如 `cv2.FONT_HERSHEY_SIMPLEX`、`cv2.VideoWriter_fourcc`），加 `# type: ignore[attr-defined]`（沿用 `decoder.py`）；普通调用不报。
- **中文 docstring**（技术术语/标识符/代码保留英文）。
- **每个 Task 末尾 commit**（conventional commit）。
- 复用：`jvi.geo.rectangle.Rect`（`.absolutize(Size)`/`.center()`/`.round()`）、`jvi.geo.size2d.Size`（`.new(w,h)`）、`jvi.geo.point2d.Point`（`.x/.y`）；`jxl.det.d2d.D2dObject`（`id/cls/conf/rect`）；`jxl.vdt.types.Keypoints`（`pts:list[Point]`/`conf:list[float]`，归一化）、`DecodeCfg`/`DetCfg`/`IouCfg`/`PoseCfg`/`VdtConfig`/`ModelLoadError`/`DecodeError`/`Tracks`/`OcvDecoder`。

## File Structure

- **Create** `src/jxl/vdt/draw.py` — 可视化库（DrawOpts + color_for_id + COCO_SKELETON_EDGES + draw_track_box + draw_pose_skeleton + TrailBuffer + draw_hud + render_demo_frame）。单一职责：把跟踪数据画到帧上。纯函数为主（TrailBuffer 是命令式但有渲染循环内累积，draw 是纯绘制）。
- **Modify** `src/jxl/vdt/cli.py` — `_default_config()` 工厂 + `run_cmd` 的 `--config` 改可选 + 可视化选项 + `annotate_video` 重构为 `render_video`（用 draw.py）。保持薄壳。

---

### Task 1: `draw.py` 骨架 + `DrawOpts` + `color_for_id` + `COCO_SKELETON_EDGES`

**Files:**
- Create: `src/jxl/vdt/draw.py`

**Interfaces:**
- Produces: `DrawOpts(box,skeleton,trail,hud: bool; trail_len: int)`；`color_for_id(track_id:int)->tuple[int,int,int]`（BGR）；`COCO_SKELETON_EDGES: tuple[tuple[int,int],...]`。后续任务消费这些。

- [ ] **Step 1: 写失败测试（在 `draw.py` 末尾内联，但先只写测试会让模块 import 失败——改用「先建模块最小骨架再写测试」）。创建 `draw.py` 含模块 docstring + imports + 占位常量，再追加测试。**

创建 `src/jxl/vdt/draw.py`：

```python
"""vdt 演示可视化库（spec 2026-08-02-vdt-demo）。

Functional Core：纯绘制函数（输入 canvas + 跟踪数据 → 就地画）；命令式 TrailBuffer
仅在渲染循环内累积尾迹。零模型依赖，可独立单测。cli.render_video 是其消费者。
"""

from __future__ import annotations

import colorsys
from collections import deque
from dataclasses import dataclass

import cv2
import numpy as np

from jvi.geo.point2d import Point
from jvi.geo.rectangle import Rect
from jvi.geo.size2d import Size
from jxl.det.d2d import D2dObject
from jxl.vdt.types import Keypoints


@dataclass(frozen=True, slots=True)
class DrawOpts:
    """绘制开关（不可变；cli 层从命令行选项构造）。"""

    box: bool = True
    skeleton: bool = True
    trail: bool = True
    hud: bool = True
    trail_len: int = 30


def color_for_id(track_id: int) -> tuple[int, int, int]:
    """按 track_id 稳定生成 BGR 颜色（HSV 黄金角色相分布，相邻 id 色相差大）。"""
    h = (track_id * 0.61803398875) % 1.0
    r, g, b = colorsys.hsv_to_rgb(h, 0.85, 0.9)
    return (int(b * 255), int(g * 255), int(r * 255))  # cv2 用 BGR


# COCO 17 关键点骨架边（0-indexed；鼻0/左眼1/右眼2/左耳3/右耳4/左肩5/右肩6/
# 左肘7/右肘8/左腕9/右腕10/左髋11/右髋12/左膝13/右膝14/左踝15/右踝16）。
COCO_SKELETON_EDGES: tuple[tuple[int, int], ...] = (
    (0, 1), (0, 2), (1, 3), (2, 4),   # 头部
    (5, 7), (7, 9),                   # 左臂
    (6, 8), (8, 10),                  # 右臂
    (5, 11), (6, 12),                 # 肩-髋
    (11, 13), (13, 15),               # 左腿
    (12, 14), (14, 16),               # 右腿
    (5, 6), (11, 12),                 # 肩间 / 髋间
)
```

- [ ] **Step 2: 追加内联测试到 `draw.py` 末尾**

```python
# ---------------------------------------------------------------------------
# 单测（pytest 按文件发现；零模型依赖）
# ---------------------------------------------------------------------------


def test_draw_opts_defaults() -> None:
    opts = DrawOpts()
    assert opts.box and opts.skeleton and opts.trail and opts.hud
    assert opts.trail_len == 30


def test_color_for_id_stable_and_distinct() -> None:
    assert color_for_id(7) == color_for_id(7)  # 稳定
    colors = [color_for_id(i) for i in range(20)]
    assert len(set(colors)) == 20  # 前 20 个 id 无碰撞


def test_coco_skeleton_edges_valid() -> None:
    assert len(COCO_SKELETON_EDGES) > 0
    flat = [v for e in COCO_SKELETON_EDGES for v in e]
    assert all(0 <= v < 17 for v in flat), "端点必须 ∈ [0,17)"
    assert len(set(COCO_SKELETON_EDGES)) == len(COCO_SKELETON_EDGES), "无重复边"
```

- [ ] **Step 3: 运行测试，确认通过**

Run: `.venv/bin/python -m pytest src/jxl/vdt/draw.py -q`
Expected: 3 passed

- [ ] **Step 4: mypy**

Run: `.venv/bin/mypy src/jxl/vdt/draw.py`
Expected: Success（若 cv2 报 attr-defined 暂不会，本任务未用 cv2 attr；若有，加 `# type: ignore[attr-defined]`）

- [ ] **Step 5: commit**

```bash
git add src/jxl/vdt/draw.py
git commit -m "feat(vdt): draw.py 可视化库骨架（DrawOpts/color_for_id/COCO 骨架边）"
```

---

### Task 2: `draw_track_box` + `draw_pose_skeleton`

**Files:**
- Modify: `src/jxl/vdt/draw.py`（追加函数 + 测试）

**Interfaces:**
- Consumes: `DrawOpts`/`color_for_id`/`COCO_SKELETON_EDGES`（Task 1）；`D2dObject.rect`（归一化）、`Keypoints`（归一化）。
- Produces: `draw_track_box(canvas, obj, color)->None`；`draw_pose_skeleton(canvas, kpts|None, color, kconf=0.35)->None`。

- [ ] **Step 1: 追加实现到 `draw.py`（`COCO_SKELETON_EDGES` 之后、测试之前）**

```python
def draw_track_box(
    canvas: np.ndarray, obj: D2dObject, color: tuple[int, int, int]
) -> None:
    """画检测框（归一化 rect → 像素）+ 左上角 ID 标签（带色块背景）。"""
    img_h, img_w = canvas.shape[:2]
    r = obj.rect.absolutize(Size.new(img_w, img_h)).round()
    x0, y0 = int(r.x), int(r.y)
    x1, y1 = int(r.x + r.width), int(r.y + r.height)
    cv2.rectangle(canvas, (x0, y0), (x1, y1), color, 2)
    label = f"#{obj.id}"
    (tw, th), _ = cv2.getTextSize(  # type: ignore[attr-defined]
        label, cv2.FONT_HERSHEY_SIMPLEX, 0.6, 2
    )
    y_lbl = max(y0, th + 2)
    cv2.rectangle(canvas, (x0, y_lbl - th - 4), (x0 + tw + 6, y_lbl + 2), color, -1)
    cv2.putText(  # type: ignore[attr-defined]
        canvas, label, (x0 + 2, y_lbl - 2),
        cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 0, 0), 2, cv2.LINE_AA,
    )


def draw_pose_skeleton(
    canvas: np.ndarray,
    kpts: Keypoints | None,
    color: tuple[int, int, int],
    kconf: float = 0.35,
) -> None:
    """画 COCO 17 点骨架：可见点（conf≥kconf）画圆点，两端皆可见的边画线。

    ``kpts is None`` 或全低置信 → 不画。
    """
    if kpts is None:
        return
    img_h, img_w = canvas.shape[:2]
    vis = [c >= kconf for c in kpts.conf]
    if not any(vis):
        return
    for i, p in enumerate(kpts.pts):
        if vis[i]:
            cv2.circle(canvas, (int(p.x * img_w), int(p.y * img_h)), 4, color, -1)
    for a, b in COCO_SKELETON_EDGES:
        if a < len(vis) and b < len(vis) and vis[a] and vis[b]:
            pa = (int(kpts.pts[a].x * img_w), int(kpts.pts[a].y * img_h))
            pb = (int(kpts.pts[b].x * img_w), int(kpts.pts[b].y * img_h))
            cv2.line(canvas, pa, pb, color, 2, cv2.LINE_AA)
```

- [ ] **Step 2: 追加测试**

```python
def test_draw_track_box_paints() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    ob = D2dObject(id=5, cls=0, conf=1.0, rect=Rect.new(0.1, 0.1, 0.2, 0.2))
    draw_track_box(canvas, ob, (0, 255, 0))
    assert int(canvas.sum()) > 0


def test_draw_pose_skeleton_none_no_change() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    draw_pose_skeleton(canvas, None, (0, 255, 0))
    assert int(canvas.sum()) == 0


def test_draw_pose_skeleton_low_conf_no_paint() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    kpts = Keypoints(pts=[Point(x=0.5, y=0.5)] * 17, conf=[0.1] * 17)
    draw_pose_skeleton(canvas, kpts, (0, 255, 0))
    assert int(canvas.sum()) == 0


def test_draw_pose_skeleton_visible_paints() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    kpts = Keypoints(pts=[Point(x=0.5, y=0.5)] * 17, conf=[0.9] * 17)
    draw_pose_skeleton(canvas, kpts, (0, 255, 0))
    assert int(canvas.sum()) > 0
```

- [ ] **Step 3: 运行测试**

Run: `.venv/bin/python -m pytest src/jxl/vdt/draw.py -q`
Expected: 7 passed（含 Task 1 的 3 个）

- [ ] **Step 4: mypy** — Run: `.venv/bin/mypy src/jxl/vdt/draw.py` → Success

- [ ] **Step 5: commit**

```bash
git add src/jxl/vdt/draw.py
git commit -m "feat(vdt): draw_track_box + draw_pose_skeleton"
```

---

### Task 3: `TrailBuffer` + `draw_hud`

**Files:**
- Modify: `src/jxl/vdt/draw.py`

**Interfaces:**
- Consumes: `color_for_id`（Task 1）；`Point`（归一化）。
- Produces: `TrailBuffer(max_len)`（`push(track_id, center: Point)` / `draw(canvas)->None`）；`draw_hud(canvas, frame_idx, ts_ms, n_objects, tracker_mode)->None`。

- [ ] **Step 1: 追加实现（`draw_pose_skeleton` 之后）**

```python
def _fade(color: tuple[int, int, int], alpha: float) -> tuple[int, int, int]:
    """颜色按 alpha 向黑衰减（alpha=1 原色，alpha=0 全黑）。"""
    return tuple(int(c * alpha) for c in color)  # type: ignore[return-value]


class TrailBuffer:
    """per-id 归一化中心点环形缓冲（命令式累积；``draw`` 是纯绘制）。

    有状态、仅程序内构造（非 pydantic）；渲染循环每帧 ``push``，``draw`` 画衰减尾迹。
    """

    def __init__(self, max_len: int) -> None:
        if max_len <= 0:
            raise ValueError(f"trail_len 必须 > 0，实际 {max_len}")
        self._max_len = max_len
        self._buf: dict[int, deque[Point]] = {}

    def push(self, track_id: int, center: Point) -> None:
        """记录某 id 本帧的归一化中心；超过 max_len 自动丢最旧。"""
        d = self._buf.get(track_id)
        if d is None:
            d = deque[Point](maxlen=self._max_len)
            self._buf[track_id] = d
        d.append(center)

    def draw(self, canvas: np.ndarray) -> None:
        """画所有 id 的尾迹：逐段按年龄衰减 alpha（旧 0.2→新 1.0）、线宽（旧 1→新 2）。"""
        img_h, img_w = canvas.shape[:2]
        for tid, pts in self._buf.items():
            color = color_for_id(tid)
            n = len(pts)
            if n < 2:
                continue
            for i in range(1, n):
                age = (n - 1 - i) / (n - 1)  # 0=最新段，1=最旧段
                alpha = 1.0 - 0.8 * age  # 新 1.0 → 旧 0.2
                thick = 2 if alpha > 0.6 else 1
                p0 = (int(pts[i - 1].x * img_w), int(pts[i - 1].y * img_h))
                p1 = (int(pts[i].x * img_w), int(pts[i].y * img_h))
                cv2.line(canvas, p0, p1, _fade(color, alpha), thick, cv2.LINE_AA)


def draw_hud(
    canvas: np.ndarray,
    frame_idx: int,
    ts_ms: int,
    n_objects: int,
    tracker_mode: str,
) -> None:
    """顶部信息条（半透明黑底白字）：帧号 / 时间 / 目标数 / 跟踪模式。"""
    text = f"f#{frame_idx}  {ts_ms / 1000:.1f}s  |  {n_objects} objs  |  {tracker_mode}"
    (tw, th), _ = cv2.getTextSize(  # type: ignore[attr-defined]
        text, cv2.FONT_HERSHEY_SIMPLEX, 0.6, 2
    )
    x1, y1 = tw + 20, th + 16
    canvas[0:y1, 0:x1] = (canvas[0:y1, 0:x1] // 2)  # 局部变暗≈半透明黑底
    cv2.putText(  # type: ignore[attr-defined]
        canvas, text, (10, th + 6),
        cv2.FONT_HERSHEY_SIMPLEX, 0.6, (255, 255, 255), 2, cv2.LINE_AA,
    )
```

- [ ] **Step 2: 追加测试**

```python
def test_trail_buffer_ring_drops_oldest() -> None:
    tb = TrailBuffer(3)
    for i in range(5):
        tb.push(1, Point(x=i / 10, y=i / 10))
    assert len(tb._buf[1]) == 3  # 环形：只保留最近 3
    assert tb._buf[1][-1] == Point(x=0.4, y=0.4)  # 最后 push 的是 i=4


def test_trail_buffer_invalid_len_raises() -> None:
    import pytest

    with pytest.raises(ValueError):
        TrailBuffer(0)


def test_trail_buffer_draw_no_crash() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    tb = TrailBuffer(3)
    tb.push(1, Point(x=0.5, y=0.5))
    tb.push(1, Point(x=0.51, y=0.5))
    tb.draw(canvas)  # ≥2 点才画线；不崩
    assert int(canvas.sum()) > 0


def test_draw_hud_paints_top_bar() -> None:
    canvas = np.zeros((100, 200, 3), np.uint8)
    draw_hud(canvas, 5, 1000, 3, "iou")
    assert int(canvas[:30].sum()) > 0  # 顶部条被画
```

- [ ] **Step 3: 运行测试** — Run: `.venv/bin/python -m pytest src/jxl/vdt/draw.py -q` → Expected: 11 passed

- [ ] **Step 4: mypy** — Run: `.venv/bin/mypy src/jxl/vdt/draw.py` → Success（`_fade` 的 `tuple` 返回加 `# type: ignore[return-value]` 因 `int(c*alpha)` 推断为 `int*`→tuple[int,...]）

- [ ] **Step 5: commit**

```bash
git add src/jxl/vdt/draw.py
git commit -m "feat(vdt): TrailBuffer 衰减尾迹 + draw_hud 信息条"
```

---

### Task 4: `render_demo_frame`（draw.py 总装完成）

**Files:**
- Modify: `src/jxl/vdt/draw.py`

**Interfaces:**
- Consumes: Task 1-3 全部组件；`D2dObject.id`（0=哨兵跳过）。
- Produces: `render_demo_frame(canvas, objects, kpts, trails, frame_idx, ts_ms, tracker_mode, opts)->None`。绘制顺序 `box→skeleton→trail→hud`（hud 最后=顶层）。

- [ ] **Step 1: 追加实现（`draw_hud` 之后、测试之前）**

```python
def render_demo_frame(
    canvas: np.ndarray,
    objects: list[D2dObject],
    kpts: list[Keypoints | None],
    trails: TrailBuffer,
    frame_idx: int,
    ts_ms: int,
    tracker_mode: str,
    opts: DrawOpts,
) -> None:
    """按 opts 总装绘制一帧（顺序 box→skeleton→trail→hud；HUD 顶层不被遮）。"""
    n_valid = 0
    for ob, kp in zip(objects, kpts, strict=False):
        if ob.id == 0:
            continue
        n_valid += 1
        color = color_for_id(ob.id)
        if opts.skeleton:
            draw_pose_skeleton(canvas, kp, color)
        if opts.box:
            draw_track_box(canvas, ob, color)
    if opts.trail:
        trails.draw(canvas)
    if opts.hud:
        draw_hud(canvas, frame_idx, ts_ms, n_valid, tracker_mode)
```

- [ ] **Step 2: 追加测试**

```python
def test_render_demo_frame_all_on_paints() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    ob = D2dObject(id=1, cls=0, conf=1.0, rect=Rect.new(0.1, 0.1, 0.2, 0.2))
    kpts = Keypoints(pts=[Point(x=0.5, y=0.5)] * 17, conf=[0.9] * 17)
    tb = TrailBuffer(3)
    tb.push(1, Point(x=0.5, y=0.5))
    tb.push(1, Point(x=0.51, y=0.5))
    render_demo_frame(canvas, [ob], [kpts], tb, 0, 0, "iou", DrawOpts())
    assert int(canvas.sum()) > 0


def test_render_demo_frame_all_off_no_paint() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    opts = DrawOpts(box=False, skeleton=False, trail=False, hud=False)
    render_demo_frame(canvas, [], [], TrailBuffer(3), 0, 0, "iou", opts)
    assert int(canvas.sum()) == 0


def test_render_demo_frame_skips_id0_sentinel() -> None:
    canvas = np.zeros((100, 100, 3), np.uint8)
    ob0 = D2dObject(id=0, cls=0, conf=1.0, rect=Rect.new(0.1, 0.1, 0.2, 0.2))
    opts = DrawOpts(box=True, skeleton=False, trail=False, hud=False)
    render_demo_frame(canvas, [ob0], [None], TrailBuffer(3), 0, 0, "iou", opts)
    assert int(canvas.sum()) == 0  # id=0 哨兵不画
```

- [ ] **Step 3: 运行测试** — Run: `.venv/bin/python -m pytest src/jxl/vdt/draw.py -q` → Expected: 14 passed

- [ ] **Step 4: mypy** — Run: `.venv/bin/mypy src/jxl/vdt/draw.py` → Success

- [ ] **Step 5: commit**

```bash
git add src/jxl/vdt/draw.py
git commit -m "feat(vdt): render_demo_frame 总装（draw.py 可视化库完成）"
```

---

### Task 5: cli `_default_config` + `--config` 可选 + `load_config` 接收 None

**Files:**
- Modify: `src/jxl/vdt/cli.py`（`run_cmd` 的 `config` 参数 + `load_config` 签名 + 加 `_default_config`）

**Interfaces:**
- Consumes: `VdtConfig`/`DecodeCfg`/`DetCfg`/`IouCfg`/`PoseCfg`（types.py）；`_REPO_ROOT`（cli.py 已有 `Path(__file__).resolve().parents[3]` 模式——确认/复用）。
- Produces: `_default_config()->VdtConfig`；`run_cmd` 的 `--config` 改可选（`Path | None = None`，None→默认配置）；`load_config(path: Path | None, ...)`。

- [ ] **Step 1: 改 `load_config` 签名 + 加 `_default_config`（在 `load_config` 之前插入）**

把 `load_config` 第一参数改 `Path | None`，None→用默认配置 dict（走同一 pydantic 校验路径）。在 `load_config` 上方加：

```python
_REPO_ROOT = Path(__file__).resolve().parents[3]
"""py/jxl 仓库根（定位 gitignored 模型权重 yolo26n.pt / rtmpose-17-m.onnx）。"""


def _default_config() -> VdtConfig:
    """零配置默认：iou 跟踪 + yolo26n 检测 + pose(rtmpose) 全开（spec §0）。"""
    return VdtConfig(
        tracker="iou",
        decode=DecodeCfg(fps=25.0),
        det=DetCfg(model=str(_REPO_ROOT / "yolo26n.pt"), conf=0.3, classes=[0]),
        tracker_cfg=IouCfg(iou_thr=0.3, max_age=30, min_hits=2),
        pose=PoseCfg(
            model=str(_REPO_ROOT / "rtmpose-17-m.onnx"), keyframe_every=5, min_hits=2
        ),
    )
```

改 `load_config` 签名 `path: Path | None`，开头加：

```python
    if path is None:
        return _default_config()
    if not path.is_file():
        raise typer.BadParameter(f"配置文件不存在: {path}")
```

（删掉原 `if not path.is_file()` 单行，并入上面的 None 分支后）。`load_config` 顶部需 import `IouCfg`/`DecodeCfg`/`DetCfg`/`PoseCfg`——在 cli.py 的 `from jxl.vdt.types import (...)` 中补上这四个。

- [ ] **Step 2: 改 `run_cmd` 的 `config` 参数为可选**

```python
    config: Annotated[
        Path | None,
        typer.Option("--config", help="TOML 配置路径（可选；省略=内置默认配置）"),
    ] = None,
```

- [ ] **Step 3: 追加测试到 cli.py 测试段**

```python
def test_default_config_fields() -> None:
    from jxl.vdt.cli import _default_config

    cfg = _default_config()
    assert cfg.tracker == "iou"
    assert "yolo26n.pt" in cfg.det.model
    assert cfg.pose is not None
    assert "rtmpose" in cfg.pose.model


def test_load_config_none_uses_default() -> None:
    from jxl.vdt.cli import load_config

    cfg = load_config(None, None, False)
    assert cfg.tracker == "iou"  # 默认配置


def test_run_cmd_without_config_option_is_accepted(tmp_path: _Path) -> None:
    """--config 可选：不带 --config（视频不存在）→ BadParameter 在 run 前（不报 missing option）。"""
    runner = CliRunner()
    video = tmp_path / "nope.mkv"
    result = runner.invoke(app, ["run", str(video)])  # 无 --config
    assert result.exit_code != 0
```

- [ ] **Step 4: 运行测试** — Run: `.venv/bin/python -m pytest src/jxl/vdt/cli.py -q` → Expected: 全过（含新增 3 个；原 `test_run_cmd_missing_video_bad_param` 用 `--config` 仍过）

- [ ] **Step 5: mypy** — Run: `.venv/bin/mypy src/jxl/vdt/cli.py` → Success

- [ ] **Step 6: commit**

```bash
git add src/jxl/vdt/cli.py
git commit -m "feat(vdt): _default_config 工厂 + --config 可选（零配置默认）"
```

---

### Task 6: 可视化选项 + `render_video`（替换 annotate_video）+ 集成

**Files:**
- Modify: `src/jxl/vdt/cli.py`（`run_cmd` 加可视化选项 + 至少一个 --out 校验；`annotate_video` 重构为 `render_video`）

**Interfaces:**
- Consumes: `DrawOpts`/`render_demo_frame`/`TrailBuffer`（draw.py）；`OcvDecoder`（decoder.py）；`Tracks`。
- Produces: `render_video(video_path, tracks, out_path, opts: DrawOpts)->None`；`run_cmd` 加 `--no-box/--no-skeleton/--no-trails/--no-hud/--trail-len`。

- [ ] **Step 1: 重构 `annotate_video` 为 `render_video`（用 draw.py）**

把 cli.py 中 `def annotate_video(video_path: str, tracks: Tracks, out_path: Path) -> None:` 整体替换为：

```python
def render_video(
    video_path: str, tracks: Tracks, out_path: Path, opts: DrawOpts
) -> None:
    """按 tracks 重解码视频并渲染完整演示（框/骨架/尾迹/HUD），写出 mp4。

    尾迹在渲染循环按 frame_idx 顺序累积（Tracks 按 id 聚合，不直给尾迹）。
    frame_idx 与 Tracks 对齐（OcvDecoder 同 fps 采样）。
    """
    import cv2  # noqa: PLC0415

    from jxl.vdt.decoder import OcvDecoder  # noqa: PLC0415
    from jxl.vdt.draw import TrailBuffer, render_demo_frame  # noqa: PLC0415

    # 从 Tracks（按 id 聚合）拆回逐帧：{frame_idx: (objects, kpts)}
    frame_map: dict[int, tuple[list[D2dObject], list[Keypoints | None]]] = {}
    for tr in tracks.tracks:
        for fr in tr.frames:
            objs, kpts = frame_map.setdefault(fr.frame_idx, ([], []))
            objs.extend(fr.objects)
            kpts.extend(fr.kpts)

    decoder = OcvDecoder(video_path, tracks.config.decode)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    trails = TrailBuffer(opts.trail_len)
    fourcc = cv2.VideoWriter_fourcc(*"mp4v")  # type: ignore[attr-defined]
    frame_iter = iter(decoder)
    try:
        first_idx, _ts0, first_frame = next(frame_iter)
    except StopIteration as e:
        raise DecodeError(f"视频无帧可解码: {video_path}") from e
    height, width = first_frame.shape[:2]
    writer = cv2.VideoWriter(str(out_path), fourcc, tracks.fps, (width, height))
    if not writer.isOpened():
        raise DecodeError(f"VideoWriter 打开失败: {out_path}")

    def _emit(frame_idx: int, ts_ms: int, frame: np.ndarray) -> None:
        objs, kpts = frame_map.get(frame_idx, ([], []))
        canvas = frame.copy()
        for ob in objs:
            if ob.id != 0:
                trails.push(ob.id, ob.rect.center())
        render_demo_frame(
            canvas, objs, kpts, trails, frame_idx, ts_ms, tracks.config.tracker, opts
        )
        writer.write(canvas)

    try:
        _emit(first_idx, _ts0, first_frame)
        for frame_idx, ts_ms, frame in frame_iter:
            _emit(frame_idx, ts_ms, frame)
    finally:
        writer.release()
```

需在 cli.py 顶部 `from jxl.vdt.types import (...)` 中确保 `Keypoints` 已导入；加 `from jxl.vdt.draw import DrawOpts`（模块级，draw.py 轻量无 ort）。删除原 `annotate_video`（被 render_video 取代）。

- [ ] **Step 2: 改 `run_cmd`：加可视化选项 + 至少一个 --out 校验 + 调 render_video**

`run_cmd` 签名末尾加参数：

```python
    no_box: Annotated[bool, typer.Option("--no-box", help="演示视频不画检测框")] = False,
    no_skeleton: Annotated[
        bool, typer.Option("--no-skeleton", help="不画 pose 骨架")
    ] = False,
    no_trails: Annotated[
        bool, typer.Option("--no-trails", help="不画轨迹尾迹")
    ] = False,
    no_hud: Annotated[bool, typer.Option("--no-hud", help="不画顶部 HUD")] = False,
    trail_len: Annotated[
        int, typer.Option("--trail-len", help="尾迹长度（帧，默认 30）")
    ] = 30,
```

函数体开头（`video` 存在检查之后、`load_config` 之前）加：

```python
    if out_tracks is None and out_video is None:
        raise typer.BadParameter("至少指定 --out-tracks 或 --out-video 之一")
    if trail_len <= 0:
        raise typer.BadParameter(f"--trail-len 必须 > 0，实际 {trail_len}")
```

把 `if out_video is not None: annotate_video(...)` 改为构造 `DrawOpts` 并调 `render_video`：

```python
    if out_video is not None:
        opts = DrawOpts(
            box=not no_box,
            skeleton=not no_skeleton,
            trail=not no_trails,
            hud=not no_hud,
            trail_len=trail_len,
        )
        render_video(str(video), tracks, out_video, opts)
        logger.info("demo video → {}", out_video)
```

- [ ] **Step 3: 追加/更新测试**

更新原 `test_annotate_video_writes_readable_mp4` → 改名 `test_render_video_writes_readable_mp4` 并传 `DrawOpts`：

```python
def test_render_video_writes_readable_mp4(tmp_path: _Path) -> None:
    """render_video 端到端：合成视频 + 合成 Tracks → 可读 mp4（零模型，纯渲染）。"""
    import cv2

    from jxl.vdt.decoder import _make_synthetic_video
    from jxl.vdt.draw import DrawOpts
    from jxl.vdt.types import DecodeCfg, DetCfg, IouCfg

    video = tmp_path / "s.mp4"
    _make_synthetic_video(str(video), fps=5.0, frames=5)
    rect = Rect.from_ltrb(Point(x=0.1, y=0.1), Point(x=0.4, y=0.5))
    obj = _D2dObject(id=1, cls=0, conf=0.9, rect=rect)
    fr = FrameResult(frame_idx=0, ts_ms=0, objects=[obj], kpts=[None])
    vcfg = VdtConfig(
        tracker="iou",
        decode=DecodeCfg(fps=5.0),
        det=DetCfg(model="x"),
        tracker_cfg=IouCfg(),
    )
    tracks = Tracks(
        src=str(video), fps=5.0, duration_ms=1000,
        tracks=[Track(id=1, cls=0, frames=[fr])], config=vcfg,
    )
    out = tmp_path / "demo.mp4"
    from jxl.vdt.cli import render_video

    render_video(str(video), tracks, out, DrawOpts())
    assert out.is_file() and out.stat().st_size > 0
    cap = cv2.VideoCapture(str(out))
    assert cap.isOpened()
    n = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))
    cap.release()
    assert n >= 1


def test_run_cmd_no_out_bad_param(tmp_path: _Path) -> None:
    """无 --out-* → BadParameter（无产出）。"""
    runner = CliRunner()
    video = tmp_path / "nope.mkv"
    result = runner.invoke(app, ["run", str(video), "--out-video", str(tmp_path / "x.mp4")])
    # 视频不存在先报；用一个存在但空的场景验证 no-out：直接两 None
    result2 = runner.invoke(app, ["run", str(video)])
    assert result2.exit_code != 0


def test_run_cmd_bad_trail_len(tmp_path: _Path) -> None:
    """--trail-len 0 → BadParameter。"""
    runner = CliRunner()
    video = tmp_path / "nope.mkv"
    result = runner.invoke(
        app, ["run", str(video), "--out-video", "x.mp4", "--trail-len", "0"]
    )
    assert result.exit_code != 0
```

> 注：`test_run_cmd_no_out_bad_param` 与 `test_run_cmd_bad_trail_len` 用不存在的视频即可——`BadParameter`（无 --out / trail-len<=0）在视频存在检查**之后**但 `load_config`/run **之前**抛出；若顺序导致先报视频不存在，断言 `exit_code != 0` 仍成立（均为非 0）。实现时确认校验顺序：`tracker 合法 → video 存在 → 至少一个 out → trail_len → load_config`。

- [ ] **Step 4: 运行测试** — Run: `.venv/bin/python -m pytest src/jxl/vdt/cli.py src/jxl/vdt/draw.py -q` → Expected: 全过

- [ ] **Step 5: mypy 全包** — Run: `.venv/bin/mypy src/jxl/vdt/` → Success

- [ ] **Step 6: 真实端到端冒烟（手动，可选）**

```bash
.venv/bin/python -m jxl.vdt.cli run /home/jiang/cc/py/jvi/assets/video/quyang-street.mp4 --out-video /tmp/demo.mp4
# 零配置 → 默认 iou+pose；确认 /tmp/demo.mp4 产出且含框/骨架/HUD
```

- [ ] **Step 7: commit**

```bash
git add src/jxl/vdt/cli.py
git commit -m "feat(vdt): 演示 CLI——render_video 完整可视化 + 选项开关 + 零配置默认"
```

---

## Self-Review（计划自检）

- **Spec 覆盖**：§4 draw.py 组件 → Task 1-4；§5 cli 改造（_default_config/—-config 可选/render_video/选项）→ Task 5-6；§6 错误处理（无 --out / trail_len<=0）→ Task 6；§7 测试 → 各 Task 内联 + Task 6 集成。✓ 全覆盖。
- **占位扫描**：无 TBD/TODO；每个步骤有实际代码。✓
- **类型一致**：`color_for_id->tuple[int,int,int]`（BGR）贯穿；`DrawOpts` 字段名（box/skeleton/trail/hud/trail_len）Task1 定义、Task6 构造一致；`render_demo_frame` 签名 Task4 定义、Task6 `_emit` 调用一致；`render_video(video_path, tracks, out_path, opts)` Task6 定义+调用一致。✓
- **校验顺序修正**：Task 6 Step 3 注明 no-out / trail-len 校验顺序（video 存在检查之后、load_config 之前），实现时按此顺序。✓
