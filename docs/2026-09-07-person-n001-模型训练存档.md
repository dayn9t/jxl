# Person 检测模型 n001 训练存档（2026-09-07）

> YOLO26n 单类 person，n001 crop640 场景特化。上接 `2026-09-06-约束双通道实验与
> 难例标注集存档.md`（标注流程与账本）；本文是训练与结果存档，与
> `2026-06-27-person模型训练部署.md`（上次训练）对照阅读。

## 1. 背景与目标

用 n001 crop640 全流程标注集（五模型共识 + 三层仲裁 + 人工兜底）训练 n001
场景专用 person 检测器，验收「共识标注流水线」产出的数据质量。

## 2. 数据集（dataset_v2）

| 层 | 帧 | 占比 | 标注来源 |
|---|---|---|---|
| L0 五模型全一致 | 10,437 | 75.7% | target 框（validators_all.jsonl 导出） |
| L1 低争议自动 | 2,348 | 17.0% | det_mine 级联 |
| 豆包仲裁确认 | 572 | 4.2% | 共识位置代表框 |
| 难例人工终版 | 431 | 3.1% | hardcase_labels_2_yolo（ROI 灰化修正版，416 框/95 空标） |
| **合计** | **13,788** | | **18,999 框 / 612 空标负样本** |

- 划分 8:1:1（jxl_split symlink）：train 11,030 / val 1,379 / test 1,379
- **复现**：本数据集构建流程已工具化 `consensus_dataset`（三源层合并 +
  classes.txt + data.yaml；stem 冲突 fail-fast；2026-09-08 真实数据对账
  13,788 帧/18,999 框逐层吻合本表），链路 `consensus_dataset → jxl_split →
  yolo_train`，用法见 skill `consensus-labeling` §⑨
- 单类 `names: {0: person}`；图 640×640（src1 底 1/3 灰化）
- 语义口径：96.9% 为"所有可见的人"语义，难例 431 帧含轻度合规语义（用户删 28 框）
- **v1 样本集已淘汰**（难例图片未灰化，用户发现后重建 v2）

## 3. 训练配置

| 项 | 值 |
|---|---|
| 底座 | **官方 COCO 预训练 yolo26n.pt**（用户权衡后选 A：单场景特化 / 与上次同起点可比 / 避免旧模型错误先验；自有 person.pt finetune 仅在数据极少时更优） |
| epochs / patience | 200 / 0（跑满不早停，同上次配方） |
| imgsz / batch | 640 / 32 |
| 训练机 | 本机 4060Ti 16G（与业务进程共卡，余 ~11G；**未动业务进程**） |
| 启动方式 | **Python API 直启 + setsid 脱离 CC 托管**（见 §5 坑） |

## 4. 结果

### TEST 终评（best.pt，1,379 帧 / 1,925 框）

| 指标 | 本次 | 上次生产模型 | Δ |
|---|---|---|---|
| P | **0.9653** | — | |
| R | **0.9380** | — | |
| mAP50 | **0.9715** | 0.956 | +0.016 |
| **mAP50-95** | **0.8952** | 0.720 | **+0.175** |
| 推理 | 2.4ms/图(4060Ti) | 0.5ms/图(4090) | 不同卡不可比 |

- val 曲线：epoch 3 mAP50 已 0.86+，epoch 200 val mAP50 0.967 / mAP50-95 0.890，test 与 val 一致（无过拟合裂缝）
- **核心结论：mAP50-95 +0.175 证明标注框贴合度直接转化为定位精度**——共识+仲裁+人工流程的价值量化
- 口径警告：test 集同出这套标注流程（同分布自洽验收）；跨集泛化需独立帧再验

### 产物

- `crop640_persons/runs/person_n001_v2/weights/best.pt`（5.4MB）/ `last.pt`
- `results.csv` / 曲线图 / train_batch 抽检图同目录
- 数据集 `crop640_persons/dataset/`（dataset_v2 symlink 划分）+ `dataset_v2/`（平铺母本）

## 5. 训练过程事件（复现者必读）

| 事件 | 处置 |
|---|---|
| `uv run yolo` CLI + setsid 卡死（单线程 sleeping，GPU 0 利用，dataloader fork 问题） | 改 Python API 直启（`YOLO(pt).train(...)`） |
| epoch 106 处被系统 OOM 杀（CC 托管后台任务被工具清理） | `YOLO(last.pt).train(resume=True)` + setsid 脱离托管；检查点无损续跑 95 epochs |
| pkill/pgrep -f 自匹配第 4 次踩坑：命令行**路径参数**含目标词也触发 | 明确 pid kill；已写入 skill 坑表 |

## 6. 待办与状态（2026-09-08 刷新）

1. **部署前独立泛化验证**（下一步首选）：需**未参与流程的新时段视频源**（用户提供，
   或妥协口径=现有 850 视频中最晚未入集帧——需用户裁决）。此前差集抽检口径有缺陷
   （池含语义去重丢弃的有人帧，48% 检出多为真人非误报，见 spotcheck jpg）
2. 部署替换 `/opt/howell/iap/v0.9/ias/model/person.pt`：**ONNX 已导出**
   `runs/person_n001_v2/person_n001_v2.onnx`（9.8MB，ModelContract 已嵌入，
   export_yolo_with_contract --imgsz 640 --conf 0.25）——替换为外向动作待用户拍板
3. 全自动标注框架启动：`docs/2026-09-06-全自动标注工具框架存档.md` §8 五个待裁决项
   （L4 工具化首期最小闭环：约束双通道代码在 arbitration/ensemble_constrained/ 存档）
4. **Phase B 全帧轨正式放弃**（2026-09-08 用户确认，勿再启动）。背景与论证：
   - 内容 = n001 最初口径的全量运行：63,088 关键帧 → 时序去重 49,726 → la 分时打标 →
     五模型共识 → 仲裁，7 阶段管线（script/n001-pipeline.sh）
   - 停摆于 stage3a 23,252/49,726：s4 双机分担遇 Xid 79 两次掉卡 → 全转本机 16G
     共卡挤不下五模型 → la 单模型分时跑到一半时工作重心转 crop640
   - 放弃成立：crop640 轨道已交付同一目标（ROI 裁切+前景去重 13,788 干净帧 →
     test mAP50-95 0.895，旧产 +0.175）；跑完剩余 53% 只得更大但更冗余的同分布集，
     边际价值低于数天 GPU+API 成本
   - 遗产可复用：管线脚本与全部工具、Phase A 校准结论（docs/2026-09-03-n001-phaseA-结果.md）、
     Xid/分时架构教训（skill 坑表）
5. （低优先）sgcc 老数据集基线重跑 ~12h（rfdetr 票修复后）

## 7. 质量终态（2026-09-08 全面审核后）

- 全量测试 **214 passed / 3 skipped**（显式理由）+ **ruff 全仓绿**（此前 38 错）
- 全面审核 18 findings / 17 fixed（含 yolo_coreset -100% 数据损失 bug、测试红 5 天、
  vdt 165 测试不可见等 6 个 high）；细节见 commit 917d929
- n001 数据目录清理 22G 纯垃圾（47G→25G）+ README.md 落位；stem `__`→`_` 全家桶迁移
  （57.9 万文件 + 35 jsonl 同步，校验 ALL PASS，回滚资产 /tmp/stem_migration_*）
